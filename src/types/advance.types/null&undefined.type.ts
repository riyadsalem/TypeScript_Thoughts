/*
// null , undefined

let user: string;

// console.log(user); // undefined

let saveButton : HTMLElement | null = document.getElementById('save'); /// null >> not found 
if(saveButton === null) console.log('NULL')

let loggedInUsername : string;

const users = [
   {name: 'Riyad', age:25},
   {name: 'Obby', age: 36}
]

const loggenInUser = users.find((user) => user.name === loggedInUsername);
// console.log(loggenInUser?.age)
*/